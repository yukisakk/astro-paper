---
export interface Props {
  id?: string;
  href: string;
  class?: string;
  ariaLabel?: string;
  title?: string;
  disabled?: boolean;
}

const {
  id,
  href,
  class: className = "",
  ariaLabel,
  title,
  disabled = false,
} = Astro.props;
---

{
  disabled ? (
    <div
      id={id}
      class:list={["group flex", className]}
      title={title}
      aria-disabled={disabled}
    >
      <slot />
    </div>
  ) : (
    <a
      id={id}
      {href}
      class:list={["group flex hover:text-accent", className]}
      aria-label={ariaLabel}
      title={title}
    >
      <slot />
    </a>
  )
}
